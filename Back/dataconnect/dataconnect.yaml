specVersion: "v1beta"
serviceId: "dataconnect"
location: "europe-west3"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "tomexplore"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
